# Example environment file
# Provide full WebSocket URL for production deployment.
# For Netlify, create a site env variable VITE_WS_URL pointing to your deployed WS endpoint.
# e.g. wss://your-chat-server.example.com
VITE_WS_URL=
